import styled from "styled-components";

const Container = styled.div`
  width: 250px;
  height: 650px;
  background-color: rgba(124, 203, 101, 0.2);
  position: sticky;
  top: 0px;
  left: 0;
`;

const Category = styled.div`
  width: 100%;
  margin: 0 auto;
`;

export { Category, Container };
